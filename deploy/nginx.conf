# HTTP 服务（初始部署用，SSL 配置完成后可注释掉）
server {
    listen 80;
    server_name gousen.com www.gousen.com 124.222.237.103;

    # SSL 证书验证用
    location /.well-known/ {
        root /var/www/html;
    }

    # 静态资源缓存
    location /css/ {
        proxy_pass http://127.0.0.1:3000;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location /js/ {
        proxy_pass http://127.0.0.1:3000;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location /images/ {
        proxy_pass http://127.0.0.1:3000;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 主站反向代理
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ===== SSL 配置（DNS 生效后取消注释） =====
# server {
#     listen 80;
#     server_name gousen.com www.gousen.com;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name gousen.com www.gousen.com;
#
#     ssl_certificate     /etc/nginx/ssl/gousen.com.crt;
#     ssl_certificate_key /etc/nginx/ssl/gousen.com.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml;
#     gzip_min_length 512;
#     gzip_vary on;
#
#     location /css/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /js/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /images/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
